// Routine tracking feature entry
import 'package:flutter/material.dart';

class RoutineTrackingPage extends StatelessWidget {
  const RoutineTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Routine Tracking Features'),
      ),
      body: const Center(
        child: Text('Routine Tracking Features Here.'),
      ),
    );
  }
}
