// Routine management feature entry
import 'package:flutter/material.dart';

class RoutineManagementPage extends StatelessWidget {
  const RoutineManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Routine Management Features'),
      ),
      body: const Center(
        child: Text('Routine Management Features Here.'),
      ),
    );
  }
}
